# Copy to .env.local and fill in. .env.local is gitignored — never commit real keys.
#   cp .env.example .env.local
# Then verify without printing any secret:
#   npm run check:keys

# ── The brain — required for Lorem to answer at all ───────────────────────────
# Two providers. BRAIN picks one; leaving it unset uses Anthropic, unless only
# an OpenRouter key is present, in which case it uses that.
#
# They are NOT interchangeable at the wire level and the route keeps separate
# request/response paths for them: the system prompt is a top-level field vs a
# first message, tools are {name, input_schema} vs {type:"function",
# function:{...parameters}}, forced-tool syntax differs, and the returned
# arguments are a parsed object vs a JSON string. Changing BRAIN is safe;
# hand-editing one path is not.
#
# BRAIN=anthropic   (default)
# BRAIN=openrouter
BRAIN=anthropic

# https://console.anthropic.com/settings/keys
# Set a monthly spend cap in the console too: this endpoint is public.
ANTHROPIC_API_KEY=

# https://openrouter.ai/keys — needed only when BRAIN=openrouter.
# Set a credit limit on the key itself; this endpoint is unauthenticated.
OPENROUTER_API_KEY=

# The model, whichever brain is selected. Leave unset to take that provider's
# default: claude-sonnet-5 on Anthropic, google/gemini-3.7-flash on OpenRouter.
#
# Sonnet is the right call for a spoken conversation — the visitor is waiting
# in silence, so time-to-first-word matters more than the last few points of
# composition quality. claude-opus-5 trades the other way.
#
# google/gemini-3.7-flash: 1.05M context, $0.375/M in and $1.875/M out, and it
# supports the forced tool call this whole design rests on. Verify any
# replacement lists
# "tools" and "tool_choice" in its supported_parameters on
# https://openrouter.ai/api/v1/models — a model without them cannot drive this
# route at all, because the guardrail reads a tool call, never free text.
BOO_MODEL=claude-sonnet-5

# ── Voice ─────────────────────────────────────────────────────────────────────
# "elevenlabs"  recommended — and already Boo's established voice (see below)
# "browser"     free, no key, but robotic and inconsistent across browsers
# "openai"      OpenAI's TTS endpoint; fine, but you already have an EL voice
#
# NOT offered here: OpenAI Realtime (speech-to-speech). See
# data/workpacks/t60-amazon-conv-commerce/decisions/2026-07-10-voice-engine-realtime.md
# — S2S audio "can't be made deterministic", so the spoken track would be
# model-paraphrased. Boo's whole guardrail is that a number can't be invented;
# with S2S the screen stays safe and the VOICE doesn't. Cascade keeps both.
BOO_VOICE_PROVIDER=elevenlabs

# ── Voice in (speech-to-text) ────────────────────────────────────────────────
# Voice IN. "elevenlabs" = Scribe (accurate, and works in Firefox/Safari where
# the browser recognizer doesn't). "browser" = Web Speech, free but weaker.
# With Scribe on, the browser recognizer still runs where available purely to
# drive the live caption — Scribe's transcript is the one Claude receives.
BOO_STT_PROVIDER=elevenlabs
# scribe_v2 only. scribe_v1 is still accepted but ElevenLabs marks it Deprecated
# ("outclassed by v2 models") — it is not a fallback worth having.
ELEVENLABS_STT_MODEL=scribe_v2

# ── ElevenLabs ────────────────────────────────────────────────────────────────
# https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=
# Boo's existing voice — the same one used by aria-portfolio and saathi. Keep it
# unless you're deliberately re-casting; a portfolio and its predecessor sounding
# like two different people is a tell.
ELEVENLABS_VOICE_ID=X03mvPuTfprif8QBAVeJ
# eleven_v3 for LIVE turns. It is the expressive model and the only one that
# accepts audio tags, which is how Lorem gets warmth into a line without
# claiming a feeling in the words.
#
# This is only affordable because the audio path streams end to end. Measured
# on a 44-word answer through the route: ~510ms to first audio, ~5.4s to the
# complete file, against roughly fifteen seconds to speak those words, so
# generation stays well ahead of playback. Buffered, the same request costs six
# seconds of silence per turn.
#
# ⚠ Two things hold that up, and breaking either silently restores the wait:
# /api/voice must call the /stream endpoint, and useSpeech must feed
# MediaSource rather than await res.blob().
#
# eleven_flash_v2_5 is the fast alternative (~135ms) and loses the tags.
# eleven_turbo_v2_5 is DEPRECATED — ElevenLabs marks it "outclassed by Flash".
ELEVENLABS_MODEL=eleven_v3

# Model for the pre-rendered fixed lines (scripts/prerender-voice.mjs). Latency
# is irrelevant here because it is paid once at build time, so this should stay
# on the best model available regardless of what the live model is set to.
ELEVENLABS_PRERENDER_MODEL=eleven_v3

# ── OpenAI (only when BOO_VOICE_PROVIDER=openai) ──────────────────────────────
# https://platform.openai.com/api-keys
OPENAI_API_KEY=
OPENAI_TTS_MODEL=gpt-4o-mini-tts
# alloy · echo · fable · onyx · nova · shimmer
OPENAI_TTS_VOICE=alloy

# ── Analytics ─────────────────────────────────────────────────────────────────
# Microsoft Clarity project id — session replay, heatmaps, rage/dead clicks.
# https://clarity.microsoft.com → Settings → Setup → the 10-char id in the tag.
#
# Public by design (it ships in the page source on every analytics tool), so
# this is NEXT_PUBLIC_ and is not a secret. Set it in Vercel's PRODUCTION scope
# only: app/components/Clarity.tsx additionally refuses to load anywhere but
# dineshrevunuru.com, so previews and localhost cannot pollute the heatmaps.
#
# Leave blank to disable tracking entirely — the component renders null.
NEXT_PUBLIC_CLARITY_PROJECT_ID=

# ── Local development only ────────────────────────────────────────────────────
# Points the Claude call at a mock upstream. Leave unset in real use.
# BOO_API_URL=
